export const getStyles = theme => ({
  wrapper: {
    display: 'flex',
    justifyContent: 'space-between',
    flexGrow: 0,
    flexShrink: 0,
    flexBasis: 'auto',
    paddingTop: 15,
    paddingBottom: 15,
  },
});
